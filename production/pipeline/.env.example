# Copy to .env in repo root (or export in shell). Never commit real keys.
# Used by a future pipeline:generate script or your own batch tool.

# Example: Replicate
# REPLICATE_API_TOKEN=

# Example: Runway
# RUNWAY_API_SECRET=

# Example: generic
# VIDEO_PROVIDER_API_KEY=
